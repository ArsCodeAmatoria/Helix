import { DocumentsScreen } from "@/components/forms/documents-screen";

export default function SjpPage() {
  return (
    <DocumentsScreen
      initialFilter="SJP"
      title="Safe Job Procedures"
      subtitle="SJPs for rigging, blind lifts & stripping"
    />
  );
}
